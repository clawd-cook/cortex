# PRD：Cortex 产品重规划（PARA + GTD + 程序员日常）

> 状态：规划草稿（本任务）  
> 分支：`cursor/para-gtd-product-replan-7d67`（基于最新 `master`）  
> 前置：`PRD-cortex.md`（V1）、`PRD-cortex-para.md`（工作流迭代）、`.trellis/spec/frontend/para-workflow.md`（已落地 first-cut）  
> 边界：这是**产品功能要点 + 展现形式**的重规划，不是再画一套滴答模块。不做第二大脑 / 笔记库 / 团队协作。

---

## 1. Summary

Cortex 要从「长得像滴答的本地待办」变成「程序员靠它过完一天」的个人执行系统。PARA 管东西放哪，GTD 管下一步做什么，日历仍是同一条任务的日期投影。本文件重定功能要点与主界面结构，刻意摆脱任务 / 日历 / 习惯三模块并列的滴答壳。

---

## 2. Contacts

| 姓名 | 角色 | 说明 |
| --- | --- | --- |
| 仓库所有者 | 产品负责人、第一用户 | 用真实打断日验收：抓住 → 处理 → 今天动手 |
| 待定 | 设计 | 工作流优先壳层、处理模式、项目页 |
| 待定 | 工程 | 在现有 Task / List 模型上演进，不另起日历对象 |

评论：单人产品。联系人补齐后只改本表。

---

## 3. Background

### 这是什么

Cortex V1 学的是滴答交互事实：一件事只存一次；列表与日历读同一张表。PARA first-cut 已在代码落地：`List` = 项目；收集箱 / 下一步 / 今天是过滤器（见 `para-workflow.md`）。

但**展现仍是滴答壳**：

- 左轨：任务 / 日历 / 习惯（倒数占位）
- 任务三栏 + 侧栏智能清单 + 底栏迷你月历
- 月历右侧「安排任务」无日期队列、格内习惯条

程序员真实日子不是「先选模块」，而是：**打断进来 → 没空就放下 → 固定时段清空 → 早上只看今天承诺 → 项目不能空转**。

### 为什么是现在

1. V1 与 PARA first-cut 已能每天打开；再加深日历画布解决不了「项目卡住」和「今天像第二个收集箱」。
2. 用户明确要求：基于 PARA + GTD + 程序员日常，**重新规划功能要点和展现**，脱离滴答束缚。
3. UI 已在收细选中态、墨纸主题（Web Interface Guidelines 审阅任务）；正适合换壳，而不是继续堆模块。

### 代码事实（已核实，不必再问用户）

| 事实 | 位置 |
| --- | --- |
| 无 `processed` / `actionable` / 独立 `projectId` | `src/types.ts` |
| 收集箱 ≈ 无项目 + 无日期 + open + 非习惯 | `src/lib/filters.ts` |
| 有任意日期即离开收集箱 | 同上 + `para-workflow.md` |
| 左轨仍是任务 / 日历 / 习惯 | `Shell.tsx` |
| 习惯是 `kind: habit`，不是 PARA 一类 | `types.ts` / `#/habits` |

### 最近有没有新技术让这变容易

没有。变容易的是：同一张任务表已经证明「一件事一个对象」。难的是**把壳从模块切换改成工作流切换**，以及补上「处理 / 承诺」语义，而不毁掉现有日历拖拽。

---

## 4. Objective

### 目标

让被打断的程序员在一个工作日里稳定完成：抓住 → 处理 → 执行 → 收尾。打开 Cortex，默认落在「今天承诺」，而不是「任务模块首页」。

对产品：Cortex = 本地执行操作系统，不是滴答精简版。  
对用户：脑子只记「下一刀」，不记「东西在哪个模块」。

### 对齐策略

- PARA：项目（有结束线）为主容器；领域 / 资料延后；归档软存在。
- GTD：收集与处理分开；下一步是视图不是桶；今天 = 日期承诺。
- 程序员日常：两分钟现在做、等待中、缺下一步可见、独立下一步允许存在。
- 展现：工作流优先壳；日历是工具视图，不是一级身份。
- Web Interface Guidelines：轻选中、可见焦点、URL 反映状态、长文截断、中文文案保持自然（不做英文 Title Case）。

### Key Results

- **KR1（习惯，第 3 周）**：连续 5 个工作日中 ≥4 天，约定处理时段把收集箱清到 0（自报）。
- **KR2（项目，第 3 周）**：所有进行中项目 100% 至少有一条未完成可动手下一步；缺则侧栏 / 项目页可见。
- **KR3（今天，第 4 周）**：打开「今天」，每条都能回答「为什么是今天」；未处理收集项不会混进今天。
- **KR4（壳，第 4 周）**：默认进入工作流首页（今天），不再默认「任务模块 + 迷你月历主导注意力」；日历从工具入口进入。
- **KR5（兼容）**：旧数据可迁；关应用再开归属仍在；V1 月历拖拽改期回归通过。

---

## 5. Market Segment(s)

市场按要完成的工作划分。

### 主市场：被打断的程序员

一天同时涌进 Slack、PR、整包需求、告警。需要立刻抓住，但不能每条当场做完。工作以项目对外交付，以下一步动手。

约束：电脑前为主；数据本地；愿花约 10 分钟清空收集箱；不愿先学完整方法论。

### 次市场：工作与生活同一套盒子

健康、账单等多为领域。V2.0 可不做领域，但不能强迫为生活琐事伪造项目。

### 本阶段不做

- 资料库 / 双向链接 / 读书笔记
- 团队看板、指派、站会
- 把「今天」当无限收集箱且从不定日期的人（可帮处理，不为该习惯优化）

---

## 6. Value Proposition(s)

| 工作 | 现在（滴答形 + first-cut） | 重规划后 |
| --- | --- | --- |
| 打断抓住 | 进收集箱或带日期，语义混 | 现在做 / 放进收集箱，处理前不分类 |
| 清空脑子 | 侧栏一堆智能清单 | 固定处理模式，一次一条 |
| 今天开工 | 今天 ≈ 日期交集，仍像清单 App | 今天 = 承诺动作，按项目分组 |
| 整包需求 | 项目与行动易双写 | 项目盒子 + 盒内下一步，同一条 |
| 看闲忙 | 切到日历模块 | 日历是排期工具，不抢走「今天」主权 |

价值曲线上 Cortex 要赢的四条：收集/处理分开；下一步是视图；今天是承诺；本地 + 同一任务对象。不赢：主题市场、微信收集、协作、资料库。

相对滴答：弱化模块矩阵与习惯/番茄堆叠；强化项目—行动回路与处理仪式。

---

## 7. Solution

### 7.1 UX / 展现形式（脱离滴答壳）

#### 原则

1. **工作流 > 模块**：一级导航表达「抓住 / 处理 / 今天 / 项目」，不是「任务 / 日历 / 习惯」。
2. **日历降级为工具**：从今天或项目「看排期」进入；保留月/周能力，但不再用左轨与迷你月历抢默认注意力。
3. **习惯降级为叠加**：习惯是特殊 `kind`，叠在今天底部；不再与任务并列成一级产品身份（除非日后单独验证需求）。
4. **轻选中、少圆饼**：延续已合并的细环 / 下划线语言（Web Guidelines：对比清晰但不粗暴）。
5. **URL 仍是真相**：每个工作流视图可深链（已有 hash 路由，需对齐新 IA）。

#### 建议壳层（V2 目标态）

```
┌ 主列：工作流 ─────────────────────────┐
│ 今天（默认）                            │
│ 收集箱（未处理 >0 时强调）               │
│ 处理（模式，可从收集箱进入）             │
│ 下一步                                  │
│ 项目（进行中；缺下一步打点）             │
│ ───────────────────────────────────   │
│ 工具：日历 · 摘要 · 搜索 · 设置         │
│ 归档：已完成 · 放弃 · 垃圾桶            │
└────────────────────────────────────────┘
```

对比滴答壳要改掉的：

| 滴答形（现状） | Cortex 目标 |
| --- | --- |
| 左轨任务/日历/习惯三模块 | 无「模块身份」左轨；工作流列表为主 |
| 侧栏底迷你月历常驻 | 迷你月历改为可选或仅日历工具内 |
| 月历右栏「安排任务」像第二收集箱 | 无日期队列归属「下一步未排期」，不叫安排任务模块感 |
| 今天按来源清单分组 | 今天按项目分组 + 独立组 |
| 从今天添加 → 易变相收集 | 从今天添加 = 已处理独立下一步且日期=今天 |

详细线框与组件取舍见同目录 `design.md`。

### 7.2 Key Features（功能要点）

沿用并升级 `PRD-cortex-para.md` 的回路，本重规划强调**优先级与展现绑定**：

| ID | 要点 | 为何对程序员 |
| --- | --- | --- |
| F1 | 三种东西：收集项 / 项目 / 下一步 | 打断时不造假项目 |
| F2 | 项目容器 + 下一步归属（视图） | 消灭双写 |
| F3 | 收集箱处理模式（一次一条） | 固定仪式清空脑子 |
| F4 | 现在做（两分钟） | Slack「帮看 PR」当场了 |
| F5 | 项目页 + 缺下一步警告 | 防止项目静默烂掉 |
| F6 | 今天按项目分组；今天≠收集箱 | 早上只看承诺 |
| F7 | 等待中（薄） | 等 CI / 等人时不骗自己 |
| F8 | 旧数据向导 | 迁出现有平级清单用法 |
| F9 | 工作流优先壳（本文件新增） | 脱离滴答模块束缚 |
| F10 | 日历作排期工具 | 闲忙可见，但不定义产品身份 |

明确砍掉（展现与范围）：

- 不复制滴答日/年/多日/多周画布矩阵
- 不做四个顶层 PARA 文件夹强迫搬家
- 不做资料库、AI 自动拆任务、微信收集
- 不把今天改成「下一步前 N 条自动灌入」

### 7.3 Technology（影响方案的部分）

| 层 | 方向 |
| --- | --- |
| 对象 | 仍同一 `Task` 表；日历不另起对象 |
| 语义升级 | 引入「已处理 / 可动手」等字段（见 PARA PRD）；`List.kind` 区分 project / area / plain |
| 路由 | 工作流视图深链；日历保留 `#/calendar/*` 作工具 |
| 存储 | `cortex:v2` 演进到 v3 需迁移说明；旧 undated+null 收集箱规则要显式迁移到「未处理」 |
| UI | 单样式表墨纸 token；选中态保持细；遵循 Web Interface Guidelines |

### 7.4 Assumptions

1. 用户会固定处理收集箱（失败则先提醒，不加分类）。
2. 「下一步是视图」学得会（侧栏保留熟悉名字与位置）。
3. 独立下一步必须存在（失败才考虑隐藏）。
4. 领域可晚做（若永不结束的「CR」项目暴增则提前）。
5. 今天必须带日期承诺（只加处理时「一键今天」，不自动灌入）。
6. **工作流壳比习惯模块更重要**（若用户强依赖习惯一级入口，再升回工具区显眼位，不回到三模块并列）。

---

## 8. Release

不写死日历日；按回路哪一段先通切。

### 阶段 A — 语义闭环（约 2–3 周）

处理 / 已处理 / 今天承诺 / 项目缺下一步 / 今天按项目分组。壳可仍三栏，但文案与默认添加语义先改对。

### 阶段 B — 展现换壳（约 +2 周）

工作流优先导航；日历进工具；迷你月历降级；安排任务面板改名/并入未排期下一步。

### 阶段 C — 领域与归档（约 +2–3 周）

领域、归档区、等待清单加强、每周回顾。

### 不进本路线图

多日/年视图、RRULE、番茄、倒数、CalDAV、系统通知矩阵、AI 拆解 — 仍走既有议题，不插队。

### 验收故事（手跑）

1. 编码中快捷记下「看支付 PR」→ 现在做 → 勾掉；收集箱仍 0。  
2. 三条没空做的进收集箱 → 处理模式清到 0（新项目+下一步 / 挂项目+今天 / 独立不设日期）。  
3. 打开今天只见承诺条，按项目分组。  
4. 勾完追问下一条；重启归属仍在。  
5. 从工具进月历拖拽改期，任务仍是那一条。

---

## Decisions log

| 日期 | 决策 | 选择 | 影响 |
| --- | --- | --- | --- |
| 2026-09-19 | 一级壳层 | **A 工作流优先** | 今天/收集箱/下一步/项目为主导航；日历与习惯进工具区；不做任务/日历/习惯三模块并列 |

---

## Appendix — 与既有文档关系

| 文档 | 关系 |
| --- | --- |
| `PRD-cortex.md` | V1 对象模型与日历仍成立 |
| `PRD-cortex-para.md` | 工作流细节多数并入；本文件加重**展现换壳**与程序员日常优先级 |
| `para-workflow.md` | 实现合同；阶段 A 会改收集箱定义时必须同步更新 spec |
| Web Interface Guidelines | 约束交互质量，不决定信息架构 |

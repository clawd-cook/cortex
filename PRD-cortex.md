# PRD：Cortex（个人任务与日程系统）

> 状态：草稿，基于 2026-09-19 对滴答清单 Web 的登录实勘  
> 范围：把「待办 + 日历 + 习惯」做成一套本地优先的桌面产品  
> 边界：学交互和信息架构，不用滴答品牌、logo、主题色

---

## 1. Summary

Cortex 是一个跑在自己电脑上的任务系统。一件事只存一次：有日期就出现在日历上，没日期就待在清单里，今天该做的会自动汇到「今天」。

第一版先做三件事：收集、排期、完成。日历、习惯、倒数纪念日后挂。不复制滴答的会员、微信提醒和协作。

---

## 2. Contacts

| 姓名 | 角色 | 说明 |
| --- | --- | --- |
| 仓库所有者 | 产品负责人 | 需求拍板、实勘账号、验收 |
| 待定 | 设计 | 壳层、任务条、三栏列表的视觉规范 |
| 待定 | 工程 | Tauri + React 实现；本地 SQLite |
| 待定 | 自己 / 第一批用户 | 用真实工作清单验收「今天」和月历 |

评论：当前没有独立团队。本文先当单人产品的施工图。联系人补齐后只改本表，不改后面的目标。

---

## 3. Background

### 这是什么

滴答清单把待办、日历、习惯、番茄、四象限放在同一个账号里。核心不是「又一个日历」，而是：**任务是唯一对象，日历只是带日期任务的投影。**

2026-09-19 登录后的真实结构：

- 左轨：日历、任务、习惯打卡、倒数纪念日、搜索
- 任务模块三栏：左清单、中列表、右详情
- 日历模块七种画布：日 / 周 / 月 / 年 / 日程 / 多日 / 多周
- 同一条任务可以出现在收集箱、今天、月网格、看板列里

本仓库 `cortex` 现在还是 Tauri + React 空模板。要从模板变成能每天用的产品。

### 为什么是现在

1. 实勘已经把路由、视图、创建、右键、筛选核对完，可以按事实写规格，不必猜。
2. 桌面端用 Tauri 可以本地存数据、全局快捷键、系统通知，这正是滴答 Web 弱、桌面强的部分。
3. 目标用户（本账号的真实用法）已经是「收集箱收事 → 项目清单分类 → 日历排期 → 今天执行」，不需要再发明流程。

### 最近有没有新技术让这件事变容易

没有魔法。变容易的是：本地数据库 + 一套任务模型，就能同时驱动列表和日历。难的是跨天条布局、拖拽改期、重复规则，这些要自己画，不要指望现成日历库长得一样。

---

## 4. Objective

### 目标

让一个人用 Cortex 完成「从想到做到做完」，而不再把脑子当备忘录。

对公司 / 项目：这是 Cortex 的第一张脸。做出来，后面的习惯、专注、AI 才有地方挂。  
对用户：打开应用 10 秒内知道今天要做什么；给任务一个日期，它就会出现在月历上。

### 对齐策略

先做「一件事一个对象」。列表、看板、日历都读同一张任务表。先个人、本地、可离线。协作、订阅日历、会员体系明确不做。

### Key Results

- **KR1（第 4 周）**：用本账号同等结构（收集箱 + 至少 2 个清单 + 标签）导入或手建 30 条任务后，「今天」视图能按清单分组列出当天未完成任务，错误率 0（不错列、不丢条）。
- **KR2（第 6 周）**：月视图能画出跨天条、今日圆点、拖一条改日期并写回存储；对照实勘：空白格创建、Esc 取消空标题不落库。
- **KR3（第 8 周）**：连续 5 个工作日，早间只打开 Cortex 的「今天」就能开始干活，不再先打开滴答看今天。以日记自报为准。
- **KR4（质量）**：关闭应用再打开，任务、日期、完成态全部还在。没有必须联网才能看自己的待办。

---

## 5. Market Segment(s)

市场按「要完成的工作」划分，不按年龄或职位。

### 主市场：把工作拆成可执行条目的人

**要做的工作：** 脑子里同时有项目、临时事、家务。需要一个入口把事抓住，再决定今天做哪几件。

**约束：**

- 主要在电脑前工作，需要桌面应用，不是只能用手机。
- 数据要在自己机器上，不能把工作待办锁在别人云上才安心。
- 愿意给任务打日期和标签，但不想先学一套复杂方法论。

**实勘证据：** 本账号「项目清单」里是 `WIP / DONE` 需求条，「下一步行动池」是今天要动手的开发任务，收集箱里还有没分类的「IDE插件打通 AICR」。这就是主市场的真实形态。

### 次市场：用日历看一整周闲忙的人

**要做的工作：** 不仅要清单，还要看见哪几天已经被任务铺满。

**约束：** 周起始、农历、节假日调休在中文工作环境里是默认预期，不是彩蛋。

### 明确不做的市场（本阶段）

- 多人共享清单、指派、评论协作
- 把 Cortex 当课表 / 公司 OA
- 靠微信公众号收任务的人（那是滴答的渠道，不是我们的产品）

---

## 6. Value Proposition(s)

### 用户要完成的工作

| 工作 | 现在怎么做 | Cortex 给什么 |
| --- | --- | --- |
| 把闪过的念头记下来 | 开滴答收集箱，或丢到聊天里 | 全局快捷键 / 收集箱，必须比打开浏览器更快 |
| 决定今天做什么 | 看「今天」智能清单 | 自动汇集：到期今天 + 今天的习惯 |
| 给项目里的需求排期 | 先写在项目清单，再拖到日历 | 同一条任务，改日期即出现在月历 |
| 回头看本周做完了什么 | 滴答「摘要」 | 按周生成已完成 / 未完成清单（V1.5） |

### 用户得到什么

- 一件事只写一次，列表和日历不会各记各的。
- 「今天」是执行面，「清单」是分类面，「日历」是时间面。三个面同一份数据。
- 关掉电脑再打开，东西还在。

### 避开什么痛

- 避免「日历里的事件」和「待办」两套系统对不上。
- 避免必须联网、必须登录才能看自己的任务。
- 避免空格子点一下就悄悄存一条没标题的垃圾任务（实勘：Esc 取消、空标题不落库）。

### 比现成工具更好的点（价值曲线）

滴答强在全平台、提醒、微信、订阅日历。Cortex 不跟这些比。

Cortex 要明显更好的三条：

1. **本地优先**：数据默认在这台电脑。
2. **任务即日历**：排期是改字段，不是复制到另一个日历。
3. **桌面手感**：全局快捷键、托盘、通知，为「一直开着」设计。

不比：主题商店、课表、共享协作、CalDAV 全家桶。

---

## 7. Solution

### 7.1 UX / 用户流

#### 应用壳（对标实勘，不抄皮肤）

```
┌──┬─────────────┬──────────────────┬─────────────┐
│轨│ 清单侧栏     │ 主画布            │ 详情 / 空态 │
│  │ 智能清单     │ 任务列表 / 日历   │             │
│  │ 普通清单     │                  │             │
│  │ 标签         │                  │             │
│  │ 已完成/垃圾  │                  │             │
│  │ 迷你月历     │                  │             │
└──┴─────────────┴──────────────────┴─────────────┘
```

左轨固定模块（V1 只亮任务和日历，其余灰置）：

| 模块 | 实勘 Hash | V1 |
| --- | --- | --- |
| 任务 | `#p/inbox/tasks`、`#q/today/tasks` | 做 |
| 日历 | `#c/all/calendar/m` 等 | 做月；周日/日程可占位 |
| 习惯 | `#q/all/habit` | V1.5 |
| 倒数 | `#countdown` | V1.5 |
| 搜索 | `/` | V1 做标题搜索 |

#### 主路径 1：抓住一件事

1. 按快捷键或点收集箱「+ 添加任务」。
2. 只打标题即可保存。清单默认收集箱。
3. 可用 `*` 日期、`!` 优先级、`#` 标签、`~` 清单（V1 可先做菜单，解析器放 V1.1）。

#### 主路径 2：今天执行

1. 打开「今天」。任务按来源清单分组（实勘：收集箱、下一步行动池，下面还可叠习惯）。
2. 勾选完成。完成条进本组或「已完成」折叠区。
3. 点标题，右侧打开详情：日期、优先级旗、备注 `/`、标签、所属清单。

#### 主路径 3：给项目任务排期

1. 打开某个普通清单（实勘：`#p/{清单ID}/tasks`，标题可带 emoji）。
2. 未完成在上，已完成折叠在下。右侧标签色块（实勘：蓝「技术改造」、粉「业务需求」）。
3. 点开详情「设置日期」，或拖到日历某一天。
4. 月历上出现色条；跨天则横贯格子，标题只在起始格。

#### 主路径 4：月历排期

1. 点空白格 → 当天创建，弹出详情卡；Esc 且标题为空则不保存。
2. 工具栏 `+` → 默认创建在今天。
3. 拖任务条到另一天 → 改日期，时长不变。
4. 拖跨天条左右边 → 改持续天数。
5. 右键：今天 / 明天 / 下周 / 选日期 / 清日期；四级优先级；移动到、标签、删除。V1 先做日期和删除，副本 / 转笔记后做。

#### 路由约定（自有，不复用滴答域名）

建议内部路由与实勘同构，便于对照：

| 页面 | 建议路由 |
| --- | --- |
| 收集箱 | `/lists/inbox` |
| 今天 | `/smart/today` |
| 明天 | `/smart/tomorrow` |
| 普通清单 | `/lists/:id` |
| 任务详情（三栏） | `/lists/:id/tasks/:taskId` |
| 月历 | `/calendar/month` |
| 周 / 日 | `/calendar/week`、`/calendar/day` |
| 看板 | `/smart/today?view=board` 或 `/lists/:id?view=board` |

实勘：智能清单是 `q`，普通清单是 `p/{id}`。筛选日历清单时 Hash 仍是 `all`，过滤是视图状态，不是另一条路由。V1 同样：日历范围用本地筛选，不必改 URL。

---

### 7.2 Key Features

#### F1. 任务对象（整个产品的地基）

一条任务包含：

| 字段 | 用途 |
| --- | --- |
| title | 列表和日历上的文案 |
| listId | 所属清单；空则收集箱 |
| start / due | 起止；只有 due 也要能落格 |
| allDay | 月视图默认不显示时刻 |
| priority | 0 无 / 1 低 / 2 中 / 3 高（产品层四级；存储可用 0–3） |
| tags[] | 列表右侧色块；日历可选按标签着色 |
| status | 未完成 / 完成 / 放弃 / 垃圾 |
| notes | Markdown；`/` 快捷插入后做 |
| completedAt | 「今天」和摘要用来分组 |

完成不是删除。已完成仍属于原清单，默认折起来。

#### F2. 清单与智能清单

**普通清单：** 用户建的分类。实勘有「项目清单」「下一步行动池」。可有 emoji、颜色。

**系统智能清单：**

| 名称 | 规则 | 侧栏 |
| --- | --- | --- |
| 今天 | due 为今天，或跨天包含今天；未完成 | 始终显示，带数量 |
| 明天 | due 为明天 | 可「有内容时显示 / 隐藏」（实勘菜单） |
| 收集箱 | 未指定清单 | 不能删、不能藏 |
| 已完成 / 已放弃 / 垃圾桶 | 按状态 | 侧栏底部 |

**本阶段不做：** 自定义智能清单、指派给我、最近 7 天（本账号侧栏也未显示，多半被藏起）。

侧栏底部迷你月历：点日期可跳到当天任务或日历日视图。格子上可打点表示有任务。

#### F3. 三栏任务视图

- 左：智能清单 + 清单组 + 标签组 + 归档。
- 中：顶栏标题、排序、更多。第一行永远是「+ 添加任务」（今天里文案是「添加任务至收集箱」）。
- 右：未选中时是空插画；选中后是详情。Hash / 路由带上 taskId。
- 列表行：勾选、标题、可选日期芯片、标签色块。选中行浅底。
- 更多菜单（实勘）：视图 = 列表 / 看板 / 时间线；隐藏已完成；显示详细；显示检查事项；打印。

V1：列表必做。看板：按清单分列即可（实勘「今天」看板就是收集箱 | 下一步行动池 | 习惯）。时间线放到 V2。

#### F4. 日历

月视图为 P0，规格以实勘为准（详见仓库旁 canvas「滴答清单月历复刻规格」）：

- 周起始可配置，本账号周一。
- 今日实心圆。邻月日期变淡。
- 周序号、农历、节假日、班/休：中文市场 P1，开关放在「日期与时间」。
- 任务条约 18px 高、圆角 3px、可勾选；跨天条标题在起始格；`ui-resizable` 表示可拖边。
- 显示设置：已完成、周末、颜色来源（清单 / 标签 / 优先级）。V1 先做按清单或按优先级一种。
- 视图下拉：日 D/1、周 W/2、月 M/3、年 Y/4、日程 A/5。V1 月必做，周次之，其余可显示但提示「即将推出」。
- 「安排任务」：右侧列出无日期任务，拖进格子即排期。这是收集箱和日历的桥梁，P0。

#### F5. 详情与创建

创建和编辑用同一张卡（实勘 class 一路：`out-detail-pop` / `task-detail`）：

- 顶：勾选、日期芯片、优先级旗
- 标题
- 备注（占位：「输入内容或使用 / 快速插入」）
- 标签 + 加号
- 底：所属清单、评论（V2）、更多（标签 / 附件 / 模板 / 转笔记）

规则：标题为空就关闭 = 不创建。有标题才写入。不要在用户按 Esc 时偷偷保存。

#### F6. 标签

标签是横切分类，不是清单。实勘标签组「任务类型」下有「技术改造」「业务需求」，列表行右侧画成药丸。点侧栏某个标签，中栏只显示带该标签的任务。

#### F7. 习惯与倒数（V1 只留接口）

实勘：「今天」会把当日习惯列在任务下面；日历显示设置里可叠习惯 / 专注 / 倒数。V1 数据模型预留 `kind=habit|countdown`，界面不画打卡环。避免第一版就分叉两套完成逻辑。

#### F8. 摘要（V1.5）

实勘 `#q/all/summary`：本周已完成 / 未完成的可编辑周报，右侧筛日期、清单、状态。第一版用「已完成时间」生成只读周报即可，不必做 AI「内容优化」。

#### F9. 搜索与快捷键

V1 最少集（来自滴答设置页，键位可改，语义要对）：

| 动作 | 建议 |
| --- | --- |
| 添加任务 | `N` / `Tab+N` |
| 今天 / 收集箱 | `G` 然后 `T` / `I` |
| 月 / 周 / 日 | `M` `W` `D` |
| 搜索 | `/` |
| 取消 | `Esc` |
| 完成 | `Tab+M` 或 `E` |
| 指令菜单 | `⌘K` |

#### F10. 明确砍掉（对标滴答但不做）

- 微信添加 / 微信提醒、微博、邮件插件
- 订阅 Google / Outlook / 飞书 / CalDAV（设置页可以留「即将推出」）
- 共享协作、指派 `@`
- 四象限独立模块（本账号左轨也没有；可用优先级 + 日期在「今天」排序代替）
- 番茄钟皮肤、40+ 主题、清单背景
- 位置提醒、艾宾浩斯、课表
- 对接滴答私有 API / Cookie 同步

若以后要迁入旧数据：只走官方 OAuth Open API，不爬登录态。

---

### 7.3 Technology

仅在影响方案时写。

| 层 | 选择 | 原因 |
| --- | --- | --- |
| 壳 | Tauri + React + TypeScript（已有） | 桌面、本地文件、全局快捷键 |
| 存储 | SQLite（via Tauri） | 单机、可备份一个文件 |
| 月历 | 自绘 CSS Grid | 跨天占位和色条是产品差异；FullCalendar 对不齐 |
| 拖拽 | `@dnd-kit` | 列表排序 + 日历落格同一套 |
| 日期 | `date-fns` 或 Temporal | 禁止在组件里手写月份进位 |
| 农历节假日 | 后挂 `lunar-javascript` + 放假表 | P1 |
| 同步 | V1 不做云同步 | 先证明本地闭环 |

任务一张表足够 V1。清单、标签、设置各一张。不要一开始就上 Event 表。

---

### 7.4 Assumptions（未验证，必须标出来）

1. **用户主要在一台电脑上用。** 若实际要手机，V1 本地桌面会失败。验证：自己用两周还要不要手机端。
2. **「今天」+ 月历能覆盖 80% 打开次数。** 看板、时间线、年视图可以晚做。验证：给壳打点，看路由分布。
3. **空标题不保存不会让人觉得「点了没反应」。** 实勘滴答就是这样做。验证：第一次可用性，看有没有人连点空白格。
4. **不需要从滴答一键导入也能开干。** 若旧数据迁不过来，KR3 会失败。验证：第 2 周决定是否做一份 CSV / 官方 API 导入。
5. **优先级四级 + 日期，够代替四象限模块。** 本账号左轨没有四象限。验证：问自己排期时会不会想念那四个格子。
6. **本地 SQLite 对「工作待办」可接受。** 若合规要求数据必须在公司云，方案要改。验证：只服务个人用途则成立。

---

## 8. Release

不写死日期。按「能开始用」来切。

### V1（约 6–8 周）——能每天打开的最小系统

- 收集箱、今天、明天、普通清单、标签
- 三栏列表 + 任务详情
- 完成 / 放弃 / 垃圾桶
- 月历：网格、色条、跨天、点格创建、拖拽改期、「安排任务」无日期面板
- 本地存储、基础快捷键、标题搜索
- 设置：周起始、显示已完成

**验收：** 把当前滴答里「项目清单 + 下一步行动池 + 收集箱」的结构手建一遍，连续 5 天只靠 Cortex 过早上。

### V1.5（约 +4 周）——中文日历与回顾

- 周视图时间轴（全天栏 + 现在红线）
- 农历、节假日、班休、周序号
- 摘要（本周已完成 / 未完成）
- 习惯打卡的只读叠层（可先不进「今天」分组）
- CSV 或官方 API 导入

### V2（更后）

- 多日 / 多周 / 年 / 日程
- 看板分组方式可配、时间线
- 重复规则（RRULE）、「显示所有重复周期」
- 倒数纪念日
- 番茄专注
- 提醒与系统通知
- 可选 iCloud / CalDAV 级只读订阅

### 不进路线图的

滴答品牌、会员墙、微信生态、多人协作、主题市场。那些是另一家公司的生意，不是 Cortex 的工作。

---

## Appendix A. 实勘对照（2026-09-19）

供实现时核对，不是给领导看的摘要。

| 表面 | 事实 |
| --- | --- |
| 收集箱 | `#p/inbox/tasks`，2 条未完成 + 已完成折叠 |
| 今天 | `#q/today/tasks`，按清单分组；可切 `#q/today/kanban` |
| 普通清单 | `#p/{id}/tasks`，点任务后 `#p/{id}/tasks/{taskId}` |
| 摘要 | `#q/all/summary`，本周已完成/未完成，可复制 |
| 习惯 | `#q/all/habit`，按上午/晚上/其他分组打卡 |
| 倒数 | `#countdown`，距离天数大数字 |
| 日历月 | `#c/all/calendar/m`，虚拟化周条 |
| 多日 | data-key `d-multi`，1 日等同日视图，hash 仍是 `d` |
| 设置 | `?modalType=settings&tabs=integrations` 等 |
| 智能清单可见性 | 有内容时显示 / 隐藏 |
| 创建卡 | 准备做什么？日期芯片、旗、收集箱、Esc 取消 |

月视图像素级规格见 canvas：`dida365-calendar-month.canvas.tsx`。

---

## Appendix B. V1 范围外的滴答能力清单

避免施工时「顺便做一点」导致永远做不完：

提醒矩阵、持续响铃、位置、艾宾浩斯、语音输入、小组件、桌面便签、课表、共享、动态、统计图表、AI 内容优化、外观主题、清单背景、订阅日历全家桶。

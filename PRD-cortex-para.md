# PRD：Cortex PARA 工作流（迭代）

> 状态：草稿，基于 V1 已上线能力与 2026-09-19 真实用法  
> 范围：让收集箱、今天、项目、下一步连成一条能每天用的回路  
> 前置：`PRD-cortex.md`（V1 任务即日历、本地优先）  
> 边界：这是任务系统的信息架构迭代，不是第二大脑 / 笔记软件

---

## 1. Summary

这一版要解决一件具体的事：新进来的工作先被抓住，有空就马上做；没空就进收集箱。固定时间清空收集箱，给该做的事定日期。今天视图只显示「今天要动手」的那些条。项目是有结束线的容器，下一步是项目里真正能动手的动作，两者必须连在一起。

V1 已经能收集、排期、完成。但「项目清单」和「下一步行动池」还是两个互不相干的桶。程序员的真实工作会被拆散，项目会卡住，今天会变成愿望清单。本迭代把 PARA 的分类方式接到 Cortex 的执行面上。

---

## 2. Contacts

| 姓名 | 角色 | 说明 |
| --- | --- | --- |
| 仓库所有者 | 产品负责人、第一用户 | 用真实工作流验收：收集 → 处理 → 今天执行 |
| 待定 | 设计 | 收集箱处理流、项目页、今天按项目分组 |
| 待定 | 工程 | 在现有任务表上加归属，不另起一套日历 |

评论：当前仍是单人产品。联系人补齐后只改本表。

---

## 3. Background

### 这是什么

PARA 把生活里的信息分成四类，按「现在能不能动手」来放，不按学科来放：

| 英文 | 中文 | 一句话 |
| --- | --- | --- |
| Projects | 项目 | 有结束线的事。做完可以打勾。 |
| Areas | 领域 | 一直要管、没有结束线的责任。 |
| Resources | 资料 | 以后可能用的参考，现在不行动。 |
| Archives | 归档 | 做过、暂停、不再管的旧东西。 |

PARA 管「东西放哪」。GTD 管「下一步做什么」。两者本来就该一起用，不是二选一。

Cortex V1 学的是滴答：一条任务、多个投影（收集箱 / 今天 / 日历）。侧栏里的普通清单是平级桶。本账号真实结构是：

- 收集箱：还没分类的事（例如「IDE 插件打通 AICR」）
- 项目清单：WIP / DONE 需求条（这是结果，不是动作）
- 下一步行动池：今天能写代码的条目（这是动作）
- 今天：带了今天日期的未完成任务，按来源清单一分组

问题很清楚：**项目和下一步没有归属关系。** 完成一条开发任务，看不出推进了哪个项目。一个项目可以长时间没有任何下一步。同一件工作常被写两遍：一遍写在项目里当标题，一遍写在行动池里当待办。

### 为什么是现在

1. V1 的收集箱、今天、清单、月历已经能每天打开。缺的不是又一种日历视图，而是工作流闭环。
2. 第一用户已经在用「收集 → 分到两个清单 → 给日期 → 看今天」。痛点每天都在：两个清单对不上。
3. GitHub `#1`–`#20` 已关闭。再往日历深度走（多日、重复规则）解决不了「项目卡住」和「收集箱堆着」。

### 最近有没有新技术让这件事变容易

没有。变容易的是：V1 已经证明「一件事只存一次」。这一版只要给任务补上「属于哪个项目」，把「下一步」和「今天」改成视图，而不是新桶。

---

## 4. Objective

### 目标

让一个人在被打断的工作日里，仍然能：先抓住事，再决定今天做什么，并看见每个进行中的项目都有下一步。

对产品：Cortex 从「长得像滴答的本地待办」变成「程序员能靠它过完一天」的操作系统。  
对用户：脑子不用同时记「有哪些项目」和「下一刀砍哪」。

### 对齐策略

继续「一件事一个对象」。列表、今天、日历仍读同一张任务表。项目是容器，不是第二种任务。今天永远是过滤器，不是第三个存放处。资料库、知识图谱、多人协作本阶段不做。

### Key Results

- **KR1（处理习惯，第 3 周）**：连续 5 个工作日中，至少 4 天能在约定的处理时段把收集箱清到 0。以日记自报为准。
- **KR2（项目不空转，第 3 周）**：所有状态为「进行中」的项目，100% 至少有一条未完成的下一步。侧栏或项目页能一眼看出「缺下一步」的项目。
- **KR3（今天是承诺，第 4 周）**：打开「今天」，每一条都能回答「为什么是今天」（用户给过日期，或处理收集箱时选了今天）。今天里不再混入未处理的收集项，除非用户明确把日期设成今天。
- **KR4（不再双写，第 4 周）**：从项目页勾完一条下一步，项目进度可见；从「下一步」视图点进去，能回到所属项目。同一件工作不必在两个清单各写一次。
- **KR5（质量）**：旧数据可迁移：原来的平级清单仍能打开；用户可以把旧「项目清单」里的条目标成项目，把旧「下一步」挂到项目上。关应用再开，归属关系还在。

---

## 5. Market Segment(s)

市场按要完成的工作划分。

### 主市场：被打断的程序员，要把项目拆成今天能写的一刀

**要做的工作：** 一天里同时涌进 Slack 消息、PR 评论、产品甩过来的整包需求、线上告警。需要立刻抓住，但不能每条都当场做完。晚上或早上固定处理一次，第二天只看今天。

**约束：**

- 主要在电脑前。收集必须比切到浏览器更快。
- 工作以「项目」为单位对外交付，以「下一步」为单位动手。两者都要，不能只留一个。
- 愿意每天花 10 分钟清空收集箱，但不愿意先学完一本方法论再开始用。
- 数据要在自己机器上。

**不是这个市场的信号：** 只想要一个共享看板、只要课表、或把 Cortex 当笔记软件。

### 次市场：把生活和工作放在同一套盒子里的人

**要做的工作：** 除了代码项目，还有健康、账单、家人约定。这些多半是领域，不是项目。

**约束：** 第一版可以只有「项目 + 下一步」。领域可以晚一点出现。不能为了理论完整，强迫用户先建四个顶层文件夹。

### 明确不做的市场（本阶段）

- 用 PARA 管文件、截图、读书笔记的人（那是资料库，不是 Cortex）
- 团队站会排期、指派、冲刺看板
- 把「今天」当成无限收集箱、从不给日期的人（产品帮他们处理，但不为这个习惯优化）

---

## 6. Value Proposition(s)

### 用户要完成的工作

| 工作 | 现在怎么做 | 这一版给什么 |
| --- | --- | --- |
| 打断进来，先抓住 | 丢进收集箱，或先记在聊天里 | 极快收集；能马上做的直接勾掉，不必建项目 |
| 没空处理时先放下 | 收集箱越堆越乱 | 固定处理流：一条条决定去哪、要不要定日期 |
| 知道今天干什么 | 看「今天」，但里面混着没处理的和真承诺 | 「今天」= 已处理且日期是今天的下一步 |
| 接到一个整包项目 | 在「项目清单」写一条标题，再去「下一步」另写任务 | 项目是盒子；盒子里长出下一步；两处看到的是同一条 |
| 防止项目静默烂掉 | 靠脑子记「这个需求还没拆」 | 进行中的项目没有下一步时，明确标红 |

### 用户得到什么

- 收集时几乎不用思考。处理时才分类。
- 项目回答「完成长什么样」。下一步回答「下一刀砍哪」。
- 今天回答「今天答应动手的是哪几刀」。日历回答「哪几天已经排满」。

### 避开什么痛

- 避免同一件事写两遍、改一处漏一处。
- 避免项目清单全是大标题、永远不知道从哪下手。
- 避免今天视图变成第二个收集箱。
- 避免「有时间就做、没时间就忘」。

### 比现成工具更好的点（价值曲线）

滴答强在清单多、日历全、提醒多。Things / OmniFocus 强在项目套行动。笔记软件强在资料。

Cortex 这一版要明显更好的四条：

1. **收集和处理分开。** 进来时只问「现在做还是放下」。分类发生在固定处理时段。
2. **下一步不是另一个桶。** 它是项目上的动作，用智能清单聚在一起。
3. **今天不是存放处。** 给日期才会出现。排期仍是改字段，和 V1 日历同一条任务。
4. **为程序员的打断设计。** 独立小动作（回 PR、回消息）不必硬造项目；整包需求必须进项目。

不比：主题、微信收集、团队协作、把所有文件自动归到 PARA 四个文件夹。

---

## 7. Solution

### 7.1 UX / 用户流

#### 信息架构（改完之后）

```
抓住 → 处理 → 执行 → 收尾

收集箱          还没决定的东西（不是 PARA 的一类）
  ├─ 现在做     两分钟内能结束：做完即勾，不建项目
  └─ 放下       等处理时段

处理收集箱      固定时间，一条条问：
  ├─ 扔掉 / 以后再说
  ├─ 这是一个项目     → 新建项目，并写第一条下一步
  ├─ 这是已有项目的动作 → 挂到项目，可选日期
  ├─ 这是独立动作     → 进入下一步（不挂项目），可选日期
  └─ 这是领域里的日常 → 挂到领域（V2.1）

项目            有结束线的盒子
下一步          智能清单：所有可动手的未完成动作
今天            智能清单：下一步里日期落在今天的子集
日历            带日期任务的投影（V1 已有）
归档            做完或暂停的项目（V2.1 可先手移）
```

侧栏建议：

```
智能
  今天        数字 = 今日未完成
  收集箱      数字 = 未处理；大于 0 时强调
  下一步      数字 = 可动手且未完成
  明天

项目          每个项目一行；缺下一步时打点
领域          V2.1
清单 / 标签   V1 保留，给还不想上 PARA 的旧数据

底部
  已完成 / 垃圾桶 / 归档
```

#### 主路径 1：打断进来（程序员最常见）

场景：正在写代码，Slack 说「这个 PR 帮看一下」。

```
快捷键 → 只打标题
         ┌─────────────┐
         │ 现在做      │  → 做完勾掉，不进收集箱（或进了立刻完成）
         │ 放进收集箱  │  → list 为空，无日期，等处理
         └─────────────┘
```

规则：收集时不逼问「这是项目还是行动」。问了就会丢掉「先抓住」这件事。

#### 主路径 2：固定时间处理收集箱

场景：下班前 15 分钟，或第二天开工前。收集箱有 8 条。

```
收集箱处理模式（一次只看一条）

  标题：IDE 插件打通 AICR
  [现在做] [挂到项目…] [变成项目] [独立下一步] [设日期] [扔掉]

键盘：
  Enter     独立下一步
  P         挂到已有项目 / 新建项目
  T         日期 = 今天 → 之后出现在「今天」
  N         日期 = 明天
  0         先不设日期（仍可在「下一步」里看见）
  Delete    扔掉
  Space     下一条
```

处理完的目标：收集箱为 0。做不到时，产品不要偷偷把剩下的标成「今天」。

#### 主路径 3：早上只看今天

场景：开工。打开 Cortex。

```
今天
  📌 项目：Cortex PARA 迭代
     ☐ 把项目-下一步的字段写进 types
  📌 项目：支付对账
     ☐ 复现账单差 2 分的单测
  · 独立
     ☐ 回张三的 PR 评论          ← 不挂项目也可以
```

分组按项目，不再按「来源清单是收集箱还是行动池」。习惯仍可叠在下面（V1.5 已有，本迭代不改）。

在今天里新建：如果用户从「今天」添加，默认日期=今天，状态=已处理的独立下一步（不是未处理收集项）。这和 V1「添加任务至收集箱且带今天日期」不同，必须改，否则 KR3 会失败。

#### 主路径 4：打过来一个项目

场景：产品说「本周把对账差账做完」。

```
1. 新建项目「支付对账差账」
   完成定义（一句话）：对账报告连续 3 天差额为 0
   可选截止日期：本周五
2. 立刻要求至少一条下一步
   例：「拉出上周差额 > 1 分的订单样本」
3. 需要今天做，就给这条下一步设今天
4. 项目出现在项目列表；下一步出现在「下一步」；有日期才出现在「今天」
```

不允许：只建项目、零下一步、还显示为「进行中」且无提示。

#### 主路径 5：做完一刀，项目还在

场景：样本拉完了。

```
勾选下一步
弹一句（可 Esc 关掉）：
  「支付对账差账」的下一步是什么？
  [写下一条] [今天先不管] [项目已经做完 → 归档]
```

这是防止项目静默烂掉的关键交互，比任何报表都重要。

#### 主路径 6：项目结束

完成定义达到 → 归档项目。底下已完成的下一步跟着进归档，不必删除。以后要翻历史，从归档打开。V2.0 可以先做「标记完成 / 移到归档清单」，完整归档区放 V2.1。

---

### 7.2 Key Features

#### F1. 分清三种东西（产品地基）

用户只需要分清这三样。领域和资料后出现。

| 东西 | 是什么 | 例子 |
| --- | --- | --- |
| 收集项 | 还没决定 | 「AICR 打通」「有人 @ 我看日志」 |
| 项目 | 有结束线的结果 | 「对账差账清零」「PARA 迭代 PRD 落地」 |
| 下一步 | 下一件能动手的动作 | 「写 types 里的 projectId」「复现差 2 分的单测」 |

**项目 vs 领域（先讲清楚，实现可后做）：**  
「Code Review」没有结束线，是领域。  
「把 Cortex PR 模板改成必须挂项目」有结束线，是项目。  
程序员最常见的误用，就是把领域写成项目，于是清单永远完不成。

#### F2. 项目是容器，下一步是任务上的归属

V1 任务字段保持。本迭代最小增量：

| 字段 | 用途 |
| --- | --- |
| `processed` | 收集箱清出的标志。未处理 = 收集项 |
| `projectId` | 可空。有值 = 属于某个项目 |
| `actionable` | 默认真。假 = 等待中（等人、等 CI），不进「下一步」和「今天」 |
| 项目 `outcome` | 一句话完成定义 |
| 项目 `status` | 进行中 / 完成 / 暂停 |

「下一步」智能清单规则：

- 未完成
- 已处理
- `actionable = true`
- 不是习惯

「今天」智能清单规则：

- 满足「下一步」规则
- 且日期落在今天（含跨天，沿用 V1）

「收集箱」规则：

- 未处理
- 未完成

一条任务可以同时：属于项目 A、出现在下一步、因为日期是今天再出现在今天。仍然是同一条。

独立下一步：`processed = true` 且 `projectId` 为空。允许存在。回一条 PR 评论不需要先开项目。

#### F3. 收集箱处理模式

普通收集箱列表仍可浏览。另提供「开始处理」（处理模式）：

- 一次一条，大标题，少按钮。
- 处理 = 设 `processed = true`，并可选项目、日期、等待。
- 清空到 0 时给一句完成反馈，不要烟花。
- 未处理数量在侧栏始终可见。大于 9 时仍显示真实数字（程序员要知道有多糟）。

固定时间：设置里可写「处理收集箱的提醒时刻」（例如 18:30）。本迭代可以只做应用内横条，系统通知走已有 V2 议题 `#26`，不在本 PRD 重做通知矩阵。

#### F4. 现在做（两分钟）

快捷添加上提供「现在做」：

- 创建后保持选中，焦点在详情或清单行。
- 用户勾完，任务进入已完成。默认不建项目、不占今天（除非当时就在今天视图里且用户需要）。
- 不强制 120 秒计时。计时是番茄的事（`#25`），这里只表达「当场结束」。

#### F5. 项目页

打开一个项目，中栏是这个盒子里的任务：

- 顶部：名称、完成定义、截止日期、状态。
- 未完成下一步在上。等待中单独一组（灰）。已完成折叠。
- 没有未完成下一步时，顶部警告：「这个项目现在没有下一步」。
- 「+」默认在本项目下创建下一步（`processed = true`）。

项目列表：进行中在上。缺下一步的项目用点或数字标出。

#### F6. 今天按项目分组

替换 V1「按来源清单分组」。组头是项目名；独立下一步落在「独立」组。

今天里改日期、完成、打开详情，行为与 V1 相同。

从今天把日期清掉：任务离开今天，仍留在下一步和所属项目。不要掉回收集箱。

#### F7. 等待中（程序员刚需，可薄做）

动作做不了、在等别人时：标为不可动手。

例子：等后端给字段、等 CI、等产品确认文案。

等待中的条目：

- 仍挂在项目上，项目页能看见
- 不出现在「下一步」「今天」
- 项目不算「缺下一步」，如果至少还有一条可动手的下一步；如果只剩等待、没有可动手的，仍提示「没有可做的下一步」

V2.0 用一个开关即可，不必做完整「等待清单」模块。

#### F8. 旧数据兼容

不强制用户重做所有清单。

- 现有普通清单继续能用。
- 提供一次性整理：把某条普通清单「升级为项目组」，或把清单里的每条任务「变成项目」（适合现在的「项目清单」这种「一条任务其实是一个项目」的用法）。
- 把另一条清单里的任务批量挂到项目（适合现在的「下一步行动池」）。
- 不做自动猜测。猜错比不迁更糟。给向导，让第一用户 10 分钟内迁完自己的真实数据。

#### F9. 明确砍掉

- 不要做成四个顶层文件夹强迫搬家。
- 不要做资料库、双向链接、读书笔记。
- 不要做自动把 Slack / 邮件扫进收集箱（收集入口仍是快捷键和输入框）。
- 不要做「AI 自动拆项目」。拆下一步是用户的思考，产品只追问，不代写。
- 不要把今天改成「所有下一步的前 N 条」。今天必须经过日期承诺。
- 不要引入第二套日历对象。

---

### 7.3 Technology

只写影响方案的部分。

| 层 | 选择 | 原因 |
| --- | --- | --- |
| 存储 | 仍是同一张任务表 + 清单表 | V1 契约不能拆成 Event / Project 两套 |
| 清单 | `List` 增加可选 `kind`: `project` / `area` / `plain` | 项目先复用清单当容器，避免新表把日历查询打乱 |
| 任务 | 增加 `processed`、`projectId`、`actionable` | 三个布尔/外键就能驱动三个智能清单 |
| 迁移 | 字段默认：旧任务 `processed = true`（已在清单里的视为处理过）；`listId = null` 的仍是收集箱且 `processed = false` | 打开升级后，已分类的不会全部掉回收集箱 |
| 日历 | 过滤条件加上「已处理」可选；默认月历仍显示全部带日期任务 | 未处理但被误标日期的条目仍要能在月历看见，方便拖走 |

项目用清单实现时：`projectId` 指向 `lists.id`，且该 list 的 `kind = project`。删除项目 = 现有删清单行为的升级版：下一步回到独立下一步，而不是收集箱（已处理过，不应退回未处理）。

---

### 7.4 Assumptions（未验证，必须标出来）

1. **用户真的会固定处理收集箱。** 若实际从不打开处理模式，项目-下一步再精巧也没用。验证：KR1。失败则先做提醒，而不是加更多分类。
2. **「下一步是视图不是桶」学得会。** 第一用户已经习惯两个平级清单。验证：自己迁数据的那 10 分钟会不会觉得「东西不见了」。失败则保留「下一步」这个名字当智能清单，侧栏位置与现在的行动池一致。
3. **独立下一步必须存在。** 若强迫每条行动都挂项目，用户会造出一堆假项目。验证：看独立组是否经常有条目。若两周为 0，再考虑隐藏。
4. **领域可以晚做。** 用户原话只点了收集箱、今天、项目、下一步。验证：若大量「Code Review」「值班」被建成永不结束的项目，就提前做领域。
5. **今天必须带日期。** 有人会希望「下一步里最重要的几条自动出现在今天」。本 PRD 拒绝。验证：KR3 自报时会不会抱怨「还要多点一次日期」。若强烈抱怨，只加「处理时一键今天」，不加自动灌入。
6. **完成下一步后追问下一条，不会烦。** 验证：连续 Esc 三次后，该项目当天不再追问。

---

## 8. Release

不写死日期。按「工作流哪一段先通」来切。

### V2.0（约 3–4 周）——回路最小可用

必须能完成用户原话里的整条路径：

1. 新任务进来：现在做，或进收集箱
2. 固定处理收集箱：挂项目 / 变项目 / 独立下一步 / 设日期
3. 今天 = 已处理且日期是今天的下一步，按项目分组
4. 项目与下一步有归属；缺下一步可见
5. 旧数据向导：项目清单 / 下一步行动池 能迁到新结构
6. 等待中开关（薄做）
7. 勾完下一步可追问「下一条是什么」

**验收故事（必须手跑一遍）：**

1. 编码中用快捷键记下「看支付 PR」，选现在做，看完勾掉。收集箱仍为 0。
2. 再记三条没空做的，全部进收集箱。
3. 处理时段：一条变新项目并写下一步；一条挂到该项目并设今天；一条独立下一步不设日期；收集箱为 0。
4. 打开今天，只能看见那条设了今天的。按项目分组正确。
5. 打开项目，能看见两条下一步。勾掉今天那条，被问下一条，写上，保存。
6. 关掉应用再打开，归属还在。

### V2.1（约 +2–3 周）——领域与归档

- 领域：无结束线的责任；下面也可以挂行动
- 归档区：完成/暂停的项目从主列表拿走，能搜回来
- 项目完成定义写在详情里，归档时核对一次
- 「只有等待、没有可做下一步」的弱提示

### V2.2（更后）

- 完整等待清单
- 每周回顾页：收集箱是否为 0、哪些项目缺下一步、本周归档了什么
- 上下文（深度工作 / 会议 / 等网），给下一步筛选，不给今天自动塞
- 资料：只读链接到文件夹或笔记，仍不是笔记软件

### 不进本迭代路线图的

多日/年视图、RRULE、番茄、倒数、CalDAV、系统通知矩阵、AI 自动拆任务。那些仍走已有 GitHub `#21`–`#27`。本 PRD 不插队。

---

## Appendix A. 程序员场景（设计时对照）

这些不是功能列表。做交互时用它们当测试剧本。

| 场景 | 正确去向 | 错误去向 |
| --- | --- | --- |
| Slack：帮看这个 PR，现在有空 | 现在做 → 完成 | 建一个「看 PR」项目 |
| Slack：帮看这个 PR，正在深挖 bug | 收集箱 | 强行写进今天 |
| 产品甩来「本周对账清零」 | 新项目 + 至少一条下一步 | 只在项目清单写个大标题 |
| 从项目拆出「先写失败单测」 | 项目内下一步；要今天做再设日期 | 再去行动池复制一条 |
| 单测写完，卡在等后端字段 | 该条标等待；项目若没有别的可做下一步，提示 | 留在今天假装能做 |
| Code Review 是职责不是项目 | V2.1 进领域；V2.0 允许独立下一步 | 建一个永不结束的「CR」项目 |
| CI 长期不稳 | 领域「交付质量」；若本周要「把 flaky 降到 0」则另开项目 | 把值班本身当成一个项目 |
| 晚上处理到一半要走 | 未处理的留在收集箱 | 系统把剩余全部标成今天 |
| 项目上线 | 归档项目，下一步历史仍可翻 | 删除所有任务，以后对不上 |

---

## Appendix B. 与 V1 PRD 的关系

| V1 仍成立 | 本迭代改掉 |
| --- | --- |
| 一件事只存一次 | 「项目清单」「下一步行动池」作为两个平级桶的用法 |
| 有日期就上日历 | 今天视图按来源清单分组 |
| 收集箱 = `listId` 为空 | 收集箱改为「未处理」；已处理的独立下一步不再叫收集项 |
| 从今天添加 → 进收集箱且带今天日期 | 从今天添加 → 已处理的独立下一步，日期=今天 |
| 删清单，任务回收集箱 | 删项目，已处理任务变独立下一步，不退回未处理 |
| 月历、跨天、拖拽改期 | 不改交互，只认新字段 |

V1 的 KR（今天不丢条、月历拖拽、本地还在）必须继续为真。本迭代验收时回归跑一遍。

---

## Appendix C. 价值曲线（相对位置）

```
          收集速度   项目-行动连在一起   今天=承诺   本地离线   资料库
滴答         高            低              中         低        低
Things       高            高              高         中        低
OmniFocus    中            很高            高         中        低
笔记+PARA    低            低              低         中        很高
Cortex V2    高            高              高         很高      不做
```

Cortex 赢在：本地 + 日历仍是同一条任务 + 收集/处理分开 + 项目和行动相连。不赢资料库。
